[package]
name = "dailyplan-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
dailyplan-domain = { path = "../domain" }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
